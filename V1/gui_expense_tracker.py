sudo apt-get install python3-tk

#def submit_expense():
   # expense = entry.get()
    # Add your logic here to process the expense
  #  print("Expense submitted:", expense)
   # entry.delete(0, tk.END)  # Clear the entry field after submitting

#def build_gui():
    # Create the main window
  #  window = tk.Tk()
  #  window.title("Expense Tracker")

    # Create a label
   # label = tk.Label(window, text="Welcome to Expense Tracker!")
   # label.pack()

    # Create an entry field
   # entry = tk.Entry(window)
   # entry.pack()

    # Create a button
   # button = tk.Button(window, text="Submit", command=submit_expense)
  #  button.pack()

    # Start the main event loop
  #  window.mainloop()

#if __name__ == "__main__":
#    build_gui()
